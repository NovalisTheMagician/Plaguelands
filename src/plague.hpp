#ifndef PLAGUE_HPP_
#define PLAGUE_HPP_

#include <stdint.h>

#include <optional>

#include <vector>
#include <string>
#include <map>

typedef uint64_t ulong;
typedef uint32_t uint;
typedef uint16_t ushort;
typedef uint8_t byte;

#endif
